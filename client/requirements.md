## Packages
date-fns | Formatting timestamps for display
framer-motion | Smooth animations for page transitions and list items
recharts | Visualizing pipeline data (counts per stage)
lucide-react | Beautiful icons (already in stack but confirming usage)
clsx | Utility for conditional classes
tailwind-merge | Utility for merging tailwind classes

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["Inter", "sans-serif"],
  display: ["Cal Sans", "Inter", "sans-serif"],
  mono: ["JetBrains Mono", "monospace"],
}
